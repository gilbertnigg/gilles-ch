title: Typ
icon: tag
num: zero

sections:
  pages:
    type: pages
    label: Work
    query: page('page://RZnIQZ3LcLZUa7n6').childrenAndDrafts.filterBy('type', page.uuid, ',')
    sortBy: 'date desc'
    info: "{{ page.date.toDate('d.m.Y') }}"
    template: work
    limit: 9999
    image:
      back: black
